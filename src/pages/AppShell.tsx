import { StyleGuide } from '@/pages/StyleGuide'

/**
 * 단일 앱 셸 — Phase 1에서 헤더 / 경로 / 메뉴 / 서브메뉴 / 컨텐츠 / 챗봇 4단 레이아웃을 구현한다.
 * 지금은 Phase 0 셋업 검증용 스타일 가이드를 띄운다.
 */
export function AppShell() {
  return <StyleGuide />
}
