# 미완 체크리스트 (2026-07-26 기준)

사용자 지시 중 **아직 안 됐거나 일부만 된 것**만 모았습니다.
각 항목은 코드에서 실제로 확인한 결과입니다. (완료된 지시는 하단 부록)

---

## 🔴 안 된 것

| # | 지시 | 현재 상태 | 해야 할 일 |
|---|---|---|---|
| 1 | **Formisch 적용** (`ui.shadcn.com/docs/forms/formisch`) | `FormFields.tsx`를 만들어 두고 **어디에도 연결하지 않음**. FormModal은 자체 검증(`lib/validate.ts`)을 씀 | FormModal을 Formisch 폼으로 교체(useModalForm + TextField), 자체 검증 제거 |
| 2 | **모든 폼 요소 validation** | FormModal(생성·초대 모달 8종)만 적용 | 로그인·회원가입·2FA·초기 셋업 3단계·업무 요청 시트·설정 모달·문서 작성기 제목까지 확대 |
| 3 | **DB만 빼고 다 돌아가게** | 스토어는 만들었으나 **모달의 "만들기/초대" 버튼이 스토어를 호출하지 않음**(닫히기만 함) | 폴더 추가→addFolder, 파일 업로드/새 파일→addFile, 멤버 추가→addMember, 업무 요청 시트→addTask, 자료 요청→addReq 연결 |
| 4 | **챗봇형 질문 입력란 전부 textarea 2줄** | 챗봇 패널만 적용. **AI 검색 시트·추론 검색 모달·채팅 룸 검색은 여전히 한 줄 input** | AiChatSheet(707행)·SearchModal(729행) textarea 2줄로 |
| 5 | **Sidebar를 React Aria(base) 버전으로** | Radix 기반 shadcn Sidebar로 적용함 (`components.json`의 base=radix) | base 전환은 프로젝트 전체 primitives 교체(migrate-radix-to-base)라 별도 결정 필요 — 진행 여부 확인 |

## 🟡 일부만 된 것

| # | 지시 | 적용된 곳 | 남은 곳 |
|---|---|---|---|
| 6 | **Resizable** | 문서저장소·채팅·업무·에코 문항/증빙/자료요청·멤버 (7) | 대시보드(본문↔위젯), 에코 요약(문항↔우측), 전체화면 뷰어(문서↔챗봇), 문서 작성기(목차↔본문) |
| 7 | **표 정렬 규칙**(헤더 가운데/수치 우측/가변 문장 좌측) | 멤버·시스템·증빙자료·자료요청·문서 목록 | 업무 목록, 에코 요약 문항 목록, 대시보드 표, Task 미리보기 모달 |
| 8 | **인풋·버튼 높이 34px 통일** | 멤버·증빙자료·자료요청·에코 요약 | 문서 저장소(폴더/파일 검색 줄), 채팅(룸 검색·대화 검색), 업무(검색), 설정 모달, 초기 셋업 |
| 9 | **shadcn Chart 적용 가능한 곳** | 대시보드 에코 진행, 증빙 커버리지 | 대시보드 KPI 스파크라인·문서 파이프라인, 에코 요약 영역별 진행률, 스토리지 게이지 |
| 10 | **모든 UI/UX shadcn 적극 활용** | Sidebar·Resizable·Chart·RadioGroup·Field·Collapsible·DropdownMenu | 자체 Modal/Sheet → shadcn Dialog/Sheet, 자체 탭 → Tabs, 자체 토글 → Switch, 진행바 → Progress, 목록 → ScrollArea, 표 → Table |

## ✅ 방금 고친 것

- **선택 칩이 라디오로 안 보이던 문제** — ChoiceRow에 실제 라디오 인디케이터(○/●) 표시. 선택 시 파란 채움이 아니라 연한 배경 + 라디오 체크

---

## 부록 — 완료 확인된 지시

시안 116항목 구현 · 검수 플로팅바 · 해시 딥링크 · 자료요청 3분할 · 멤버 4탭 · 에코 요약/증빙 시안 정렬 ·
실제 PDF/XLSX/DOCX 생성 및 문서 보기 연결 · 확대·축소 실동작 · 아이콘 중복 제거 · 업무 카드 우측 라인 ·
챗봇 접기 핸들 잘림 · 전체 메뉴 닫기 위치 · 패널 핸들 사각형(radius 4·아이콘 정중앙) · 드롭다운 자동 위/아래 ·
드롭다운 폭=트리거 폭 + 최소 폭 고정 · 셀렉트 텍스트 좌측/화살표 우측 · 사이드바 그룹 접기(기본 펼침) ·
시스템 그룹 우측 팝오버 · 사이드메뉴 검수 제거 · 파일 상세 탭 가로 100% · 헤더 배지 N · shadcn MCP/skill 설치
