# Fable 검수 QA — Opus가 놓친 것 체크리스트 (2026-07-26)

검수 인덱스 115항목 전수 순회 + 규칙별 자동 측정(버튼 쪼개짐·높이·정렬·네이티브 select·글리프 아이콘·깨진 이미지·가로 넘침)과
기능 실측(검증·확대축소·팝오버·해시)을 다시 돌린 결과입니다.

## 🔴 새로 발견 — Opus가 놓친 것

| # | 위치 | 문제 | 실측 근거 | 상태 |
|---|---|---|---|---|
| 1 | 멤버 초대 모달 행 | **같은 행 안에서 높이 3종 혼재** — 이메일 인풋 39px / 역할 셀렉트 36px / 삭제 버튼 34px. "인풋과 버튼 높이는 같아야 함" 규칙 위반 | getBoundingClientRect 39/36/34 | ✅ 고침 — 34/34/34 재실측 확인 |
| 2 | FormModal 전체 | 모달 폼 인풋이 전부 39px (`inputCls`의 py-2.5) — 본문 화면은 34px로 통일했는데 모달만 다름 | 생성·초대 모달 8종 공통 | ⏸ 보류 — 모달 폼도 34px로 맞출지 결정 필요 |
| 3 | 조직 등록 · 조직&사업부 생성 | placeholder는 "2-100자"인데 **실제 길이 검증이 없음** — '조직명'은 required만, '조직&사업부 이름'은 maxLen(60) | rulesFor() 추론 규칙 확인 | ✅ 고침 — 두 필드에 minLen(2)+maxLen(100) 명시, 명시 규칙이 있으면 기본 60자 제한은 미적용. 1자→"2자 이상", 101자→"100자까지", 80자→통과 실측 |
| 4 | 전 화면+모달 24곳 | **아이콘 전용 버튼에 접근 가능한 이름 없음** (help, close, 체크박스, 확대/축소, 페이저 등) — 스크린리더가 리거처 원문("help")을 읽음 | 화면 스캔 15건 + 모달 걸어보기 9건 | ✅ 고침 — 도움말·닫기·전체/행 선택(role=checkbox+aria-checked)·확대/축소·이전/다음 페이지·첨부 제거 등 title 부여. 115항목 재스캔 잔여 0건 |

## 🟡 기존 미완 항목 — 여전히 열려 있음을 이번에 실측으로 재확인

| # | 항목 | 이번 실측 |
|---|---|---|
| 5 | 챗봇형 질문 입력 textarea 전환 (AI 검색 시트·추론 검색 모달) | 두 곳 모두 textarea 0개, 한 줄 input만 ("무엇이든 물어보세요" 외 1) |
| 6 | 표 정렬 규칙 (헤더 가운데) | 업무 목록 헤더 '업무' **left**, 대시보드 표 헤더 '문서/문항' **left**, 스토리지 수치 start — 미적용 구간 그대로 |
| 7 | 모달 CTA → 데이터 스토어 연결 | FormModal.submit()은 검증 후 close만 호출 (코드 확인) |
| 8 | Formisch(FormFields.tsx) 연결 | 여전히 어디서도 import 안 됨 (코드 확인) |

## ✅ 회귀 없음 확인 (Opus 작업분 재검증)

- 115항목 전수: 빈 화면 0건 · fresh 로드 콘솔 오류 0건 (세션 로그의 오류 3건은 편집 중 HMR 잔재 — 현재 코드 아님)
- 네이티브 `<select>` 0 · 금지 글리프(✕▼‹› 등) 문자 아이콘 0 · 깨진 이미지 0 · body 가로 넘침 0
- 확장자 텍스트 배지 잔여 0 (화면 9종 + 모달 5종 모두 FileExt 아이콘으로 교체 확인)
- 멤버 표 정렬: 이메일 left · 담당 프로젝트 center ✅
- 시스템 그룹 팝오버: `data-side="right"`, 멤버등록/채널관리(8)/설정 ✅
- 버튼 세로 쪼개짐 0 (높이 큰 5건은 탭바·items-stretch 구조로 글자는 한 줄 — 정상)
- XLSX 확대·축소 실동작: fontSize 12.6px → 17.64px ✅
- 헤더 배지 N ✅ · 초대 모달 빈 제출 차단 + 에러 문구 ✅ · 웹사이트 URL 형식 검증 ✅
- RoleCell 드롭다운: 레이어 폭 = 트리거 폭(w-full) + useFlipUp 위로 열림 (코드 확인)

> 우선순위 제안: 1 → 3 → 4 는 소규모 수정, 5 → 6 은 기존 미완 체크리스트와 함께 일괄 처리 권장.
