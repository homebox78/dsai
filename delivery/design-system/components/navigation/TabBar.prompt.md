같은 화면 안에서 정보를 갈아 끼울 때 쓴다 (새 화면 만들지 않는다).

```jsx
<TabBar variant="segment" tabs={[{id:'preview',label:'미리보기'},{id:'version',label:'버전'},{id:'activity',label:'활동'}]} value={tab} onChange={setTab} />
```
